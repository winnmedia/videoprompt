name: 02. Deployment & Production
# 통합된 배포 및 프로덕션 모니터링 파이프라인

on:
  push:
    branches: [main]
  workflow_run:
    workflows: ["01. Main CI/CD Pipeline"]
    types: [completed]
    branches: [main]
  schedule:
    # 매시간 프로덕션 헬스 체크
    - cron: '0 * * * *'

env:
  NODE_VERSION: '20'
  PNPM_VERSION: '9'

# 동시 실행 제한 (배포 안전성)
concurrency:
  group: deployment-${{ github.ref }}
  cancel-in-progress: false

jobs:
  # 🚀 Stage 1: Pre-deployment 검증
  pre-deployment-validation:
    name: '🔍 Pre-deployment Validation'
    runs-on: ubuntu-latest
    if: github.event_name == 'push' || (github.event_name == 'workflow_run' && github.event.workflow_run.conclusion == 'success')
    timeout-minutes: 10
    outputs:
      should-deploy: ${{ steps.deploy-check.outputs.should-deploy }}
      deployment-env: ${{ steps.deploy-check.outputs.deployment-env }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # 배포 전 스모크 테스트
      - name: Pre-deployment Smoke Tests
        run: |
          echo "💨 배포 전 스모크 테스트..."
          pnpm test:smoke:pre-deployment || {
            echo "❌ 배포 전 스모크 테스트 실패!"
            exit 1
          }

      # 환경변수 검증 (프로덕션)
      - name: Production Environment Validation
        run: |
          echo "🔧 프로덕션 환경변수 검증..."
          pnpm validate-env --env production --strict
        env:
          NODE_ENV: production
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          JWT_SECRET: ${{ secrets.JWT_SECRET }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}

      # 배포 결정
      - name: Deployment Decision
        id: deploy-check
        run: |
          if [[ "${{ github.ref }}" == "refs/heads/main" ]]; then
            echo "should-deploy=true" >> $GITHUB_OUTPUT
            echo "deployment-env=production" >> $GITHUB_OUTPUT
            echo "✅ 메인 브랜치 - 프로덕션 배포 승인"
          else
            echo "should-deploy=false" >> $GITHUB_OUTPUT
            echo "deployment-env=none" >> $GITHUB_OUTPUT
            echo "⏸️ 비-메인 브랜치 - 배포 건너뜀"
          fi

  # 🚀 Stage 2: 프로덕션 배포
  production-deployment:
    name: '🚀 Production Deployment'
    runs-on: ubuntu-latest
    needs: pre-deployment-validation
    if: needs.pre-deployment-validation.outputs.should-deploy == 'true'
    timeout-minutes: 15
    environment: production

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # 프로덕션 빌드
      - name: Production Build
        run: pnpm build
        env:
          NODE_ENV: production
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
          NEXT_PUBLIC_APP_URL: ${{ secrets.NEXT_PUBLIC_APP_URL }}

      # Vercel 배포
      - name: Deploy to Vercel
        uses: amondnet/vercel-action@v25
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
          vercel-args: '--prod'
          working-directory: ./

      # 배포 성공 알림
      - name: Deployment Success Notification
        run: |
          echo "🎉 프로덕션 배포 성공!"
          echo "🔗 배포 URL: ${{ secrets.NEXT_PUBLIC_APP_URL }}"

  # 📊 Stage 3: 배포 후 검증
  post-deployment-verification:
    name: '✅ Post-deployment Verification'
    runs-on: ubuntu-latest
    needs: production-deployment
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # 프로덕션 헬스 체크
      - name: Production Health Check
        run: |
          echo "🩺 프로덕션 헬스 체크..."

          # 메인 엔드포인트 체크
          curl -f ${{ secrets.NEXT_PUBLIC_APP_URL }}/api/health || {
            echo "❌ 헬스 체크 실패!"
            exit 1
          }

          # 인증 엔드포인트 체크
          curl -f ${{ secrets.NEXT_PUBLIC_APP_URL }}/api/auth/health || {
            echo "❌ 인증 헬스 체크 실패!"
            exit 1
          }

          echo "✅ 프로덕션 헬스 체크 통과"

      # API 응답 시간 체크
      - name: API Performance Check
        run: |
          echo "⚡ API 성능 체크..."

          start_time=$(date +%s%N)
          curl -s ${{ secrets.NEXT_PUBLIC_APP_URL }}/api/health > /dev/null
          end_time=$(date +%s%N)

          duration=$(( (end_time - start_time) / 1000000 ))
          echo "API 응답 시간: ${duration}ms"

          if [ $duration -gt 2000 ]; then
            echo "❌ API 응답 시간이 너무 깁니다 (${duration}ms > 2000ms)"
            exit 1
          fi

          echo "✅ API 응답 시간 양호"

      # 프로덕션 스모크 테스트
      - name: Production Smoke Tests
        run: |
          echo "💨 프로덕션 스모크 테스트..."
          BASE_URL=${{ secrets.NEXT_PUBLIC_APP_URL }} pnpm test:smoke:production || {
            echo "❌ 프로덕션 스모크 테스트 실패!"
            exit 1
          }

  # 📊 Stage 4: 프로덕션 모니터링 (스케줄 실행 시)
  production-monitoring:
    name: '📊 Production Monitoring'
    runs-on: ubuntu-latest
    if: github.event_name == 'schedule'
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # 종합 헬스 체크
      - name: Comprehensive Health Check
        run: |
          echo "🩺 종합 헬스 체크 시작..."

          # 메인 사이트
          if ! curl -f --max-time 10 ${{ secrets.NEXT_PUBLIC_APP_URL }}; then
            echo "❌ 메인 사이트 접근 불가"
            exit 1
          fi

          # API 엔드포인트들
          api_endpoints=(
            "/api/health"
            "/api/auth/health"
            "/api/planning/health"
          )

          for endpoint in "${api_endpoints[@]}"; do
            echo "Testing $endpoint..."
            if ! curl -f --max-time 10 ${{ secrets.NEXT_PUBLIC_APP_URL }}$endpoint; then
              echo "❌ $endpoint 접근 불가"
              exit 1
            fi
          done

          echo "✅ 모든 헬스 체크 통과"

      # 성능 모니터링
      - name: Performance Monitoring
        run: |
          echo "⚡ 성능 모니터링..."

          # Lighthouse CI 실행 (Core Web Vitals)
          if command -v lhci &> /dev/null; then
            lhci autorun --upload.target=temporary-public-storage --collect.settings.chromeFlags="--no-sandbox"
          fi

          echo "✅ 성능 모니터링 완료"

      # 데이터베이스 연결 체크
      - name: Database Connection Check
        run: |
          echo "🗄️ 데이터베이스 연결 체크..."

          # 간단한 연결 테스트
          if ! pnpm run db:ping; then
            echo "❌ 데이터베이스 연결 실패"
            exit 1
          fi

          echo "✅ 데이터베이스 연결 정상"

  # 🚨 알림 및 보고
  deployment-notification:
    name: '📢 Deployment Notification'
    runs-on: ubuntu-latest
    needs: [pre-deployment-validation, production-deployment, post-deployment-verification]
    if: always()

    steps:
      - name: Deployment Results Summary
        run: |
          echo "📊 배포 결과 요약"
          echo "========================"

          if [[ "${{ needs.pre-deployment-validation.result }}" == "success" ]]; then
            echo "✅ 배포 전 검증: 통과"
          else
            echo "❌ 배포 전 검증: 실패"
          fi

          if [[ "${{ needs.production-deployment.result }}" == "success" ]]; then
            echo "✅ 프로덕션 배포: 성공"
          elif [[ "${{ needs.production-deployment.result }}" == "skipped" ]]; then
            echo "⏸️ 프로덕션 배포: 건너뜀"
          else
            echo "❌ 프로덕션 배포: 실패"
          fi

          if [[ "${{ needs.post-deployment-verification.result }}" == "success" ]]; then
            echo "✅ 배포 후 검증: 통과"
          elif [[ "${{ needs.post-deployment-verification.result }}" == "skipped" ]]; then
            echo "⏸️ 배포 후 검증: 건너뜀"
          else
            echo "❌ 배포 후 검증: 실패"
          fi

      # Slack 알림 (실패 시)
      - name: Slack Notification (Failure)
        if: failure()
        uses: 8398a7/action-slack@v3
        with:
          status: failure
          channel: '#alerts-deployment'
          webhook_url: ${{ secrets.SLACK_WEBHOOK_URL }}
          text: |
            🚨 배포 프로세스 실패!
            브랜치: ${{ github.ref }}
            커밋: ${{ github.sha }}
            즉시 확인이 필요합니다.

      # Slack 알림 (성공 시)
      - name: Slack Notification (Success)
        if: success() && needs.production-deployment.result == 'success'
        uses: 8398a7/action-slack@v3
        with:
          status: success
          channel: '#deployments'
          webhook_url: ${{ secrets.SLACK_WEBHOOK_URL }}
          text: |
            ✅ 프로덕션 배포 성공!
            사이트: ${{ secrets.NEXT_PUBLIC_APP_URL }}
            브랜치: ${{ github.ref }}
            커밋: ${{ github.sha }}