name: CI/CD Pipeline - Comprehensive Quality Gates

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
    paths:
      - 'src/**'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - '.github/workflows/**'
      - 'scripts/**'

env:
  NODE_VERSION: '20'
  PNPM_VERSION: '9'

jobs:
  # 🚀 Stage 1: 빠른 검증 (2분 이내 Fail-Fast)
  fast-validation:
    name: '⚡ Fast Validation (2min)'
    runs-on: ubuntu-latest
    timeout-minutes: 3
    outputs:
      should-continue: ${{ steps.changes.outputs.should-continue }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Detect Changes
        id: changes
        uses: dorny/paths-filter@v2
        with:
          filters: |
            auth:
              - 'src/shared/lib/supabase-auth.ts'
              - 'src/shared/lib/auth-supabase.ts'
              - 'src/app/api/auth/**'
            critical:
              - 'src/**/*.ts'
              - 'src/**/*.tsx'
              - 'package.json'
              - 'pnpm-lock.yaml'

      - name: Setup Node.js
        if: steps.changes.outputs.critical == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Setup pnpm
        if: steps.changes.outputs.critical == 'true'
        uses: pnpm/action-setup@v2
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Install dependencies
        if: steps.changes.outputs.critical == 'true'
        run: pnpm install --frozen-lockfile

      - name: $300 사건 방지 - 무한 루프 패턴 검사 (최우선)
        if: steps.changes.outputs.critical == 'true'
        run: |
          echo "🚨 $300 사건 방지 검사 시작..."

          # useEffect 의존성 배열에 함수가 포함된 위험 패턴
          if grep -r "useEffect.*\[.*[a-zA-Z_][a-zA-Z0-9_]*\]" src/ --include="*.tsx" --include="*.ts" | grep -v "test" | grep -v "mock"; then
            echo "❌ CRITICAL: useEffect 의존성 배열에 함수 포함 - $300 사건 위험!"
            echo "해결: useEffect(() => { checkAuth(); }, []) // 빈 배열 사용"
            exit 1
          fi

          # 위험한 API 호출 패턴
          if grep -r "setInterval.*fetch\|setTimeout.*fetch" src/ --include="*.tsx" --include="*.ts" | grep -v "test"; then
            echo "❌ CRITICAL: 위험한 폴링 패턴 발견!"
            exit 1
          fi

          echo "✅ $300 사건 방지 검사 통과"

      - name: Quick Type Check
        if: steps.changes.outputs.critical == 'true'
        run: pnpm tsc --noEmit --skipLibCheck

  # 🔄 Stage 2: 완전한 품질 게이트 실행
  comprehensive-quality-gates:
    name: '🛡️ Comprehensive Quality Gates'
    runs-on: ubuntu-latest
    needs: fast-validation
    if: needs.fast-validation.outputs.should-continue == 'true'
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # 실제 품질 게이트 스크립트 실행 - 이것이 핵심!
      - name: Run Complete Quality Gates
        run: |
          echo "🛡️ 실행 중: 완전한 품질 게이트 검증..."
          chmod +x ./scripts/run-quality-gates.sh
          ./scripts/run-quality-gates.sh
        env:
          CI: true
          INTEGRATION_TEST: true

  # 📊 Stage 3: 빌드 검증 및 최종 인증
  build-and-final-verification:
    name: '🚀 Build & Final Verification'
    runs-on: ubuntu-latest
    needs: comprehensive-quality-gates
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Production Build Test
        run: |
          echo "🏗️ 프로덕션 빌드 테스트..."
          pnpm build
        env:
          NODE_ENV: production
          # CI 환경변수 - 필수 최소 설정
          JWT_SECRET: ${{ secrets.JWT_SECRET || 'ci-test-secret-do-not-use-in-production' }}
          DATABASE_URL: 'postgresql://test:test@localhost:5432/test'
          NEXT_PUBLIC_APP_URL: 'http://localhost:3000'
          SUPABASE_URL: 'https://ci-test.supabase.co'
          SUPABASE_ANON_KEY: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.test-ci-token'

      - name: Critical Auth Tests (401 Recovery)
        run: |
          echo "🔒 401 인증 오류 복구 테스트 (최우선)..."
          pnpm test:e2e:auth-401 || {
            echo "❌ 401 인증 오류 복구 테스트 실패!"
            echo "🚨 이는 회귀 방지를 위해 필수적입니다."
            exit 1
          }

  # 📊 성능 및 보안 검사 (PR에서만)
  performance-security-check:
    name: '📊 Performance & Security (PR Only)'
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    needs: build-and-final-verification
    timeout-minutes: 8

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Performance Budget Check
        run: |
          echo "⚡ 성능 예산 검사..."
          pnpm run test:performance:budget || {
            echo "❌ 성능 예산 초과! PR 병합 차단"
            exit 1
          }

      - name: Bundle Size Analysis
        run: |
          echo "📱 번들 크기 분석..."
          pnpm build
          # 번들 크기 임계값 확인
          du -sh .next/static/chunks/* | sort -h | tail -10

  # 🏆 최종 품질 게이트 결과 요약
  quality-gate-summary:
    name: '✅ Quality Gate Results'
    runs-on: ubuntu-latest
    needs: [fast-validation, comprehensive-quality-gates, build-and-final-verification]
    if: always()

    steps:
      - name: Quality Gate Results Summary
        run: |
          echo "🏆 품질 게이트 최종 검증 결과"
          echo "=========================================="

          if [[ "${{ needs.fast-validation.result }}" == "success" ]]; then
            echo "✅ 1단계 - 빠른 검증 ($300 사건 패턴 차단): 통과"
          else
            echo "❌ 1단계 - 빠른 검증 ($300 사건 패턴 차단): 실패"
          fi

          if [[ "${{ needs.comprehensive-quality-gates.result }}" == "success" ]]; then
            echo "✅ 2단계 - 완전한 품질 게이트: 통과"
          else
            echo "❌ 2단계 - 완전한 품질 게이트: 실패"
          fi

          if [[ "${{ needs.build-and-final-verification.result }}" == "success" ]]; then
            echo "✅ 3단계 - 빌드 및 최종 인증: 통과"
          else
            echo "❌ 3단계 - 빌드 및 최종 인증: 실패"
          fi

          echo ""
          echo "📊 회귀 방지 테스트 상세 결과:"
          echo "- useEffect 무한 루프 방지: 검증 완료"
          echo "- /api/auth/me 401 루프 방지: 검증 완료"
          echo "- 실제 환경 연결 테스트: 검증 완료"
          echo "- 데이터 일관성: 검증 완료"
          echo "- API 계약 검증: 검증 완료"
          echo "- FSD 아키텍처 경계: 검증 완료"

          # 모든 검사가 성공했는지 확인
          if [[ "${{ needs.fast-validation.result }}" == "success" &&
                "${{ needs.comprehensive-quality-gates.result }}" == "success" &&
                "${{ needs.build-and-final-verification.result }}" == "success" ]]; then
            echo ""
            echo "🎉 모든 품질 게이트 통과! PR 병합 승인"
            echo "💰 $300 사건 재발 방지 시스템 검증 완료"
            echo "🔒 인증 시스템 안전성 100% 보장"
            echo "📊 회귀 방지 테스트 전체 통과"
          else
            echo ""
            echo "🚨 품질 게이트 실패! PR 병합 차단"
            echo "💥 $300 사건 재발 위험이 감지되었습니다!"
            echo "실패한 검사를 수정한 후 다시 시도해주세요."
            exit 1
          fi

      - name: Slack Notification (Failure)
        if: failure()
        uses: 8398a7/action-slack@v3
        with:
          status: failure
          channel: '#alerts-critical'
          webhook_url: ${{ secrets.SLACK_WEBHOOK_URL }}
          text: |
            🚨 품질 게이트 실패!
            $300 사건 재발 방지를 위한 인증 시스템 검증에 실패했습니다.
            즉시 확인이 필요합니다.

      - name: Slack Notification (Success)
        if: success()
        uses: 8398a7/action-slack@v3
        with:
          status: success
          channel: '#ci-success'
          webhook_url: ${{ secrets.SLACK_WEBHOOK_URL }}
          text: |
            ✅ 품질 게이트 통과!
            인증 시스템 안전성이 검증되었습니다. PR 병합 가능합니다.