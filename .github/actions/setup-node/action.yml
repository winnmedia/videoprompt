name: 'Setup Node.js & pnpm'
description: 'Grace QA Lead용 Node.js 및 pnpm 최적화 설정'

inputs:
  node-version:
    description: 'Node.js 버전'
    required: false
    default: '20.19.0'
  pnpm-version:
    description: 'pnpm 버전'
    required: false
    default: '9.0.0'

runs:
  using: 'composite'
  steps:
    - name: 🔧 Setup pnpm
      uses: pnpm/action-setup@v4
      with:
        version: ${{ inputs.pnpm-version }}

    - name: 🔧 Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ inputs.node-version }}
        cache: 'pnpm'
        cache-dependency-path: 'pnpm-lock.yaml'

    - name: 🔍 Verify Setup
      shell: bash
      run: |
        echo "Node.js version: $(node --version)"
        echo "pnpm version: $(pnpm --version)"
        echo "✅ Grace QA: 환경 설정 완료"