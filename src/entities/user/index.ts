/**
 * User Entity Public API
 *
 * 사용자 도메인 모델의 진입점입니다.
 * 사용자 관련 비즈니스 로직과 타입을 제공합니다.
 */

// 사용자 엔티티가 생성되면 여기서 export합니다.
// 예시:
// export type { User, UserRole } from './types';
// export { UserModel } from './model';
// export { userService } from './service';

export {};